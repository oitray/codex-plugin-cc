name: Auto-Merge

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  auto-merge:
    uses: oitray/templates/.github/workflows/auto-merge.yml@main
    secrets:
      merge_token: ${{ secrets.PROJECT_TOKEN }}
